beneficiary: public(address)
def _forwardWeight(delegate_with_weight_to_forward: address):
    assert x +2
        if x > 2 :
            assert x > 2
        else:
            break
        end ;
    assert x > 2
end